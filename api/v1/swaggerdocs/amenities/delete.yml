Deletes an amenity.
---
tags:
  -  "amenities"
produces:
  - "application/json"
parameters:
  - in: path
    name: amenity_id
    description: "The ID of the Amenity to delete."
    required: true
    schema:
      type: "string"
responses:
  200:
    description: "An empty object representing the removed Amenity."
    schema:
      type: "object"
  404:
    description: "Not found"
